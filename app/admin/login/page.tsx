'use client';import {useState} from 'react';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');async function login(){const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(r.ok)location.href='/admin';else alert('Login inválido')}return <main className="grid min-h-screen place-items-center bg-dark text-white grid-bg px-4"><div className="glass w-full max-w-md rounded-3xl p-8"><h1 className="text-3xl font-black text-gold">Painel LordSMP</h1><p className="text-zinc-400 mt-2">Acesso exclusivo do dono.</p><input className="mt-6 w-full rounded-2xl bg-black/30 px-4 py-3" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="mt-3 w-full rounded-2xl bg-black/30 px-4 py-3" placeholder="Senha" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={login} className="btn-gold mt-5 w-full rounded-2xl py-3">Entrar</button></div></main>}
